<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= typeof title !== 'undefined' ? title : 'Lead Dashboard' %> | Architect Leads</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: { slate: { 850: "#172033" }, accent: { 50: "#f0f9ff", 100: "#e0f2fe", 500: "#0ea5e9", 600: "#0284c7" } },
            fontFamily: { sans: ["DM Sans", "system-ui", "sans-serif"] },
          },
        },
      };
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700&display=swap" rel="stylesheet" />
    <style>body { font-family: "DM Sans", system-ui, sans-serif; } .pipeline-dot { width: 8px; height: 8px; border-radius: 50%; }</style>
  </head>
  <body class="bg-slate-900 text-slate-100 min-h-screen">
    <div class="flex">
      <aside class="w-56 min-h-screen bg-slate-850 border-r border-slate-700 flex flex-col">
        <div class="p-5 border-b border-slate-700">
          <a href="/dashboard" class="text-lg font-semibold text-white tracking-tight">Architect Leads</a>
          <p class="text-xs text-slate-400 mt-0.5">Nurture & generate leads</p>
        </div>
        <nav class="flex-1 p-3 space-y-0.5">
          <a href="/dashboard" class="flex items-center gap-3 px-3 py-2 rounded-lg text-slate-300 hover:bg-slate-700/50 hover:text-white <%= currentPage === 'dashboard' ? 'bg-slate-700/50 text-white' : '' %>">Dashboard</a>
          <a href="/practices" class="flex items-center gap-3 px-3 py-2 rounded-lg text-slate-300 hover:bg-slate-700/50 hover:text-white <%= currentPage === 'practices' ? 'bg-slate-700/50 text-white' : '' %>">Practices</a>
          <a href="/pipeline" class="flex items-center gap-3 px-3 py-2 rounded-lg text-slate-300 hover:bg-slate-700/50 hover:text-white <%= currentPage === 'pipeline' ? 'bg-slate-700/50 text-white' : '' %>">Pipeline</a>
          <a href="/automation" class="flex items-center gap-3 px-3 py-2 rounded-lg text-slate-300 hover:bg-slate-700/50 hover:text-white <%= currentPage === 'automation' ? 'bg-slate-700/50 text-white' : '' %>">Automation</a>
        </nav>
        <div class="p-3 border-t border-slate-700 text-xs text-slate-500">Architect directory data • Lead CRM</div>
      </aside>
      <main class="flex-1 p-8 overflow-auto">
        <%- include(contentPartial) %>
      </main>
    </div>
  </body>
</html>
